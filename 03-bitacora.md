# Bitácora y Trazabilidad

## 1. Matriz de trazabilidad

| Criterio | Commit o archivos | Prueba que lo verifica | Estado |
| :--- | :--- | :--- | :--- |
| **CA-1** (Registro paciente) | `Application/Patients/PatientService.cs`, `PatientValidator.cs`, `Domain/Entities/Patient.cs`, `Api/Controllers/PatientsController.cs`, `web/src/app/patient-form/*` | `PatientServiceTests.cs` (ej. `CA1_RegisterPatient_ValidRequest...`, `CA1_RegisterPatient_DuplicateDocument...`); Interfaz: `patient-form.spec.ts` | Cubierto |
| **CA-2** (Registro contacto) | `Application/Contacts/ContactService.cs`, `ContactValidator.cs`, `Domain/Entities/Contact.cs`, `Api/Controllers/ContactsController.cs` (incluye `GET /api/patients/{patientId}/contacts`), `web/src/app/contact-form/*`, `web/src/app/contact-list/*` | `ContactServiceTests.cs` (ej. `CA2_RegisterContact_ValidRequest...`, `CA2_RegisterContact_InactivePatient...`); lista de contactos: `ContactListTests.cs`; Interfaz: `contact-form.spec.ts`, `contact-list.spec.ts`, `app.spec.ts` | Cubierto |
| **Anexo A** (Consulta combinada) | `PatientService.cs` (`ListAsync`), `PatientListItemResponse.cs`, `PatientsController.cs`, `web/src/app/patient-table/*` | `PatientListValidationTests.cs` (paginación); Interfaz: `patient-table.spec.ts`. Verificación SQL manual vía `002_datos_prueba.sql`. | Parcial |
| **CA-3** (Corrección contacto) | N/A (Modelo preparado con `IsDeleted`, `CorrectionReason`, `CorrectedAt`) | N/A | Fuera de alcance |
| **CA-4** (Filtros del mes) | N/A | N/A | Fuera de alcance |
| **CA-5** (Paciente ilocalizable) | N/A | N/A | Fuera de alcance |
| **CA-6** (Reporte adherencia) | N/A | N/A | Fuera de alcance |

## 2. Registro de decisiones y uso de IA

**Herramientas de IA:** Claude Code. Utilizado para validación de plan, generación de *boilerplate* (BD, API, pruebas, UI). Cada fase fue revisada, corregida o rechazada por mí antes de integrarse al código.

*   **2026-09-23 | Decisión:** Commits de hallazgos y plan previos al código (`3a7b27e`). **Motivo:** Cumplimiento estricto de las reglas de la prueba. **Origen:** Propia.
*   **2026-09-23 | Decisión:** Limitar alcance a CA-1, CA-2 y tabla de lectura. **Motivo:** Garantizar una rebanada vertical completa, probada y con arquitectura sólida en 5 horas. **Origen:** Propia.
*   **2026-09-23 | Decisión:** Refinar `Country` a código ISO de 2 letras validado. **Motivo:** Evitar variantes tipográficas y normalizar prefijos telefónicos. **Origen:** IA (Aprobada tras revisión).
*   **2026-09-23 | Decisión:** Validación simple de teléfono (dígitos y `+`) sin normalización E.164. **Motivo:** Optimización de tiempo al evitar dependencias externas de librerías telefónicas. **Origen:** Propia.
*   **2026-09-23 | Decisión:** Catálogos en inglés en BD, etiquetas en español en UI. **Motivo:** Estandarización del código fuente. **Origen:** Propia.
*   **2026-09-23 | Decisión:** **Rechazar `nvarchar` sugerido por la IA para usar `varchar`.** **Motivo:** `varchar` en SQL Server es suficiente y admite tildes/ñ con la collation correcta, ahorrando espacio. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** Clean Architecture ligera sin MediatR/CQRS. **Motivo:** Separación de responsabilidades clara sin sobreingeniería para el tamaño del proyecto. **Origen:** Propia asistida por IA.
*   **2026-09-23 | Decisión:** Configurar EF Core con `ValueGeneratedNever()` para booleanos. **Motivo:** Evitar que EF omita valores `false` (ej. crear paciente inactivo) al insertar. **Origen:** IA (Aprobada).
*   **2026-09-23 | Decisión:** **Corregir el `PatientService` anémico de la IA (`new Patient{...}`).** **Motivo:** Implementé `Patient.Create()` para proteger invariantes del dominio, agregué `ILogger` y eliminé una consulta redundante en el bloque `catch`. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** Filtrar `DbUpdateException` en infraestructura. **Motivo:** La IA asumía cualquier error como duplicado (`409`); se ajustó para reaccionar solo a violaciones de índice único (2601/2627). **Origen:** IA (Corregida y aprobada).
*   **2026-09-23 | Decisión:** **Rechazar la creación de contactos para pacientes inactivos.** **Motivo:** Regla de negocio lógica (el API ahora responde `400`). **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** **Invertir el orden de `CountAsync` en la paginación (`ListAsync`).** **Motivo:** La IA contaba toda la tabla antes de paginar. Lo corregí para traer la página primero y omitir el `COUNT` (costoso en SQL Server) si la página no se llena. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** Centralizar llamadas HTTP en `PatientApiService`. **Motivo:** Cumplir el Anexo A y unificar configuración de URLs en Angular. **Origen:** IA (Aprobada).
*   **2026-09-23 | Decisión:** **Rechazar el uso exclusivo del proxy de desarrollo de Angular; se configuró CORS en .NET.** **Motivo:** El proxy solo resuelve el entorno local, omitir CORS rompe la integración real en un despliegue. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** UI valida solo campos requeridos; lógica de negocio delegada al API. **Motivo:** Evitar duplicidad y desfase de reglas de validación. **Origen:** IA (Aprobada).
*   **2026-09-23 | Decisión:** **Corregir fugas de memoria en RxJS sugerido por IA.** **Motivo:** La IA dejó `subscribe()` abiertos; implementé `takeUntilDestroyed` y cancelación de peticiones previas (`switchMap`/abort) en la tabla. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** **Rechazar la interfaz de una sola pantalla que armó la IA.** La pantalla inicial es solo la lista de pacientes con un botón "Nuevo paciente"; registrar paciente, registrar contacto y ver todos los contactos se abren en ventanas modales, y la tabla muestra solo el último contacto de cada paciente. **Motivo:** La pantalla estaba revuelta y hay que cuidar la UI/UX. **Origen:** IA (**Rechazada y corregida**).
*   **2026-09-23 | Decisión:** Agregar `GET /api/patients/{patientId}/contacts` (`02-plan.md` solo definía tres endpoints; cambio posterior al plan). **Motivo:** Sin él la lista completa de contactos del modal no tiene de dónde salir. Se ordena en memoria (son pocos contactos por paciente) para no depender de SQLite, que no ordena `DateTimeOffset`, y poder probarlo. **Origen:** Mía (el requisito), implementado por la IA.
*   **2026-09-23 | Decisión:** Modales sobre el elemento nativo `<dialog>` y usuario del gestor recordado en memoria entre formularios (`GestorContext`). **Motivo:** El navegador ya resuelve el foco, la tecla Esc y el fondo, y no hay que reescribir el usuario en cada formulario; no se guarda nada fuera de la pestaña. **Origen:** IA (Aprobada).
*   **2026-09-23 | Decisión:** **Consulta combinada de `Patients` y `Contacts` en `GET /api/patients` (Anexo A) y el índice que necesita a volumen real.** Se pagina primero `Patients` (`ORDER BY CreatedAt DESC, Id`, `Skip/Take`, máximo 100 por página) y solo para esas filas se calculan dos subconsultas correlacionadas por `PatientId`: el total de contactos vigentes y el último contacto (`IsDeleted = 0`, `ORDER BY ContactDate DESC`). Así se evita el problema N+1 (una consulta por paciente) y no se trae toda la tabla de contactos a memoria. **Índice:** `IX_Contacts_PatientId_ContactDate` sobre `(PatientId, ContactDate DESC) WHERE IsDeleted = 0`. Por cada paciente de la página, SQL Server entra directo a sus contactos por `PatientId`, ya ordenados por fecha: el último contacto es la primera entrada y el total se cuenta sobre el propio índice, sin leer la tabla ni ordenar. El filtro `IsDeleted = 0` lo mantiene pequeño y coincide con la condición de la consulta. **Volumen (supuesto, no medido):** con 100 000 pacientes y 2 000 000 de contactos, sin ese índice cada subconsulta recorrería la tabla de contactos completa (25 pacientes por página × 2 millones de filas); con él son 25 búsquedas de pocas filas cada una. **Evaluado y no agregado:** (1) `INCLUDE (Channel, ResultCode, GestorUsername)`: solo se necesitan de un contacto por paciente (a lo mucho 100 búsquedas por página), no compensa el espacio extra del índice. (2) `Patients (CreatedAt DESC, Id)`: sin él, el orden de la paginación recorre y ordena `Patients` en cada petición; es barato con miles de pacientes pero crece con el volumen, y es el primer índice a agregar si la medición lo muestra. (3) El desempate por `CreatedAt` del contacto no está en el índice; solo afecta a contactos con la misma fecha exacta. **Limitación:** la consulta no tiene prueba automática porque SQLite no ordena `DateTimeOffset`; se verificó a mano contra SQL Server. **Origen:** IA (**Aprobada**).
*   **2026-09-23 | Decisión:** Agregar ESLint (`angular-eslint`, con `eslint-config-prettier`) y Prettier a la interfaz, con los scripts `lint`, `format` y `format:check`. **Motivo:** Que el estilo y los errores comunes los revise una herramienta y no la vista de quien lee el código. Prettier queda como única autoridad de formato; `endOfLine: auto` evita falsos errores por saltos de línea de Windows. **Origen:** Mío (el requisito), configurado por la IA.
*   **2026-09-23 | Decisión:** **Corregir código de la IA que el linter marcó por accesibilidad.** El modal usaba `autofocus` y un `(click)` en la plantilla sin equivalente de teclado; ahora el foco inicial se pone por código al primer campo y el cierre por clic en el fondo se registra en el componente (con teclado se cierra con Esc). No se desactivó ninguna regla. **Motivo:** `no-autofocus`, `click-events-have-key-events` e `interactive-supports-focus` de `angular-eslint`. **Origen:** IA (**Corregida** por el linter).
*   **2026-09-23 | Decisión:** **Corregir el README tras probarlo en un clon limpio.** La opción A (migraciones) dejaba la base vacía y el README no lo decía; se agregó el paso de `002_datos_prueba.sql` para cargar los datos y desde dónde correrlo. **Motivo:** Un revisor que siguiera la opción A veía la lista sin pacientes. **Origen:** IA (**Corregida** al verificar el README de cero).
