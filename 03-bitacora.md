# Bitácora

## 1. Matriz de trazabilidad

| Criterio | Commit o archivos | Prueba que lo verifica | Estado |
| :-- | :-- | :-- | :-- |
| CA-1: registro de paciente por el gestor, con validación de documento duplicado | Commit `3c36e45`: `Application/Patients/PatientService.cs`, `PatientValidator.cs`, `Domain/Entities/Patient.cs`, `Api/Controllers/PatientsController.cs` (`POST /api/patients`) | `Tests/Patients/PatientServiceTests.cs`: `CA1_RegisterPatient_ValidRequest_PersistsPatientAndReturnsSuccess`, `CA1_RegisterPatient_MissingPhone_ReturnsValidationErrorOnPhone`, `CA1_RegisterPatient_DuplicateDocument_ReturnsConflictAndDoesNotInsertTwice`, `CA1_RegisterPatient_RaceConditionOnSave_ReturnsConflict` y las demás pruebas con prefijo `CA1_` | Parcial (servicio y API cubiertos; falta la interfaz) |
| CA-2: registro de contacto asociado a un paciente | Commit de la Fase 3 (hash pendiente): `Application/Contacts/ContactService.cs`, `ContactValidator.cs`, `Domain/Entities/Contact.cs`, `Api/Controllers/ContactsController.cs` (`POST /api/patients/{patientId}/contacts`) | `Tests/Contacts/ContactServiceTests.cs`: `CA2_RegisterContact_ValidRequest_PersistsContactAssociatedToPatient`, `CA2_RegisterContact_PatientDoesNotExist_ReturnsNotFoundAndDoesNotInsert`, `CA2_RegisterContact_InactivePatient_IsRejectedAndDoesNotInsert` y las demás pruebas con prefijo `CA2_` | Parcial (servicio y API cubiertos; falta la interfaz) |
| Tabla de verificación: `GET /api/patients` con consulta combinada del Anexo A (no es un CA del PRD; demuestra CA-1 y CA-2) | Commit de la Fase 4 (hash pendiente): `Application/Patients/PatientService.cs` (`ListAsync`), `PatientListItemResponse.cs`, `Api/Controllers/PatientsController.cs` | `Tests/Patients/PatientListValidationTests.cs` cubre solo los parámetros de paginación. La consulta se verifica a mano contra SQL Server con `scripts/002_datos_prueba.sql`, porque SQLite no puede ordenar por `DateTimeOffset` | Parcial (la consulta no tiene prueba automatizada) |
| CA-3: corrección de contacto | No implementado (el modelo ya trae `IsDeleted`, `CorrectionReason`, `CorrectedAt` y `ReplacedByContactId`) | No aplica | Fuera de alcance |
| CA-4: filtros del mes | No implementado | No aplica | Fuera de alcance |
| CA-5: paciente ilocalizable | No implementado | No aplica | Fuera de alcance |
| CA-6: reporte de adherencia | No implementado | No aplica | Fuera de alcance |

## 2. Registro de decisiones y uso de IA

**Herramientas de IA:** Claude Code. Lo usé para revisar los hallazgos y el plan, y para construir la base de datos y los scripts SQL, el API, las pruebas, la interfaz Angular y esta bitácora. Cada fase se propuso y yo la aprobé, corregí o rechacé antes de que se escribiera código.

Formato: fecha · decisión · motivo · propuesta de. Los rechazos y correcciones a lo que propuso la herramienta van en **negritas**.

- 2026-09-23 · Los hallazgos y el plan se commitearon antes de cualquier código (`3a7b27e`) · la prueba lo exige · yo.
- 2026-09-23 · Se corrigieron dos supuestos cruzados en los hallazgos (duplicados y corrección de registros) y se agregaron hallazgos que faltaban · los supuestos no respondían a su propio hallazgo · asistente propuso, yo aprobé.
- 2026-09-23 · Alcance cerrado en CA-1, CA-2 y una tabla de solo lectura; fuera quedan autorregistro, CA-3 a CA-6 y autenticación · el tiempo es de 5 horas y prefiero una rebanada completa con pruebas · yo.
- 2026-09-23 · Cambio posterior al plan: `Country` pasa de texto libre a código ISO de 2 letras (`CO`, `PE`, `EC`), validado · evita variantes de escritura, vuelve confiable el índice único de documento y sirve para prefijo telefónico y zona horaria · el asistente propuso; primero me resistí y luego lo acepté.
- 2026-09-23 · Cambio posterior a los hallazgos: el teléfono se valida de forma simple (dígitos con `+` opcional, de 7 a 15) en vez de normalizarlo a E.164 · por tiempo y sin librería · yo.
- 2026-09-23 · Tipos de documento Cédula, DNI y Pasaporte, sin reglas por país; los valores de todos los catálogos se guardan en inglés (`Cedula`, `Dni`, `Passport`, `Reachable`, `NoAnswer`...) y las etiquetas en español solo van en la interfaz · no conozco el contexto de otros países y hay poco tiempo · yo.
- 2026-09-23 · **Rechacé `nvarchar` que sugirió el asistente y dejé `varchar` en todo el texto** · porque en si ese tipo de dato varchar en SQL Server admite tildes y "ñ" · corrección mía.
- 2026-09-23 · Clean Architecture ligera (Domain, Application, Infrastructure, Api), sin MediatR, CQRS ni Unit of Work; los servicios usan el `DbContext` directo a través de `IAppDbContext` · separa capas sin sobreingeniería · yo, con el asistente.
- 2026-09-23 · Migraciones de EF Core aplicadas al arrancar en Development, más `scripts/001_esquema.sql` generado de ellas y `scripts/002_datos_prueba.sql` (9 pacientes, 22 contactos) · cumple lo que pide la prueba y el evaluador arranca sin pasos manuales · asistente propuso, yo aprobé.
- 2026-09-23 · `IsActive` e `IsDeleted` con valor por defecto en la base y `ValueGeneratedNever()` · sin eso EF omite `false` al insertar y la base dejaba activo a un paciente creado como inactivo · asistente propuso, yo aprobé.
- 2026-09-23 · `SET QUOTED_IDENTIFIER ON` en `002_datos_prueba.sql` · al correrlo con `sqlcmd` falló el insert en `Contacts` porque SQL Server lo exige con índices filtrados · lo detecté al ejecutar el script; el asistente corrigió.
- 2026-09-23 · Pruebas del servicio con SQLite en memoria y no con la base en memoria de EF · SQLite respeta el índice único y así se prueba el duplicado · asistente propuso, yo aprobé.
- 2026-09-23 · **Corregí el `PatientService` que propuso el asistente:** usaba `new Patient {...}`, no tenía logging y volvía a consultar la base dentro del `catch` de la condición de carrera; pedí `ILogger`, el método de fábrica `Patient.Create`, sin segunda consulta y verificación del parseo de catálogos · evitar un modelo anémico y consultas de más · corrección mía.
- 2026-09-23 · Ajuste del asistente a mi corrección: mi `catch` tomaba cualquier `DbUpdateException` como duplicado y disfrazaría otros fallos de base de datos como `409`; solo se traduce la violación de índice único (2601 y 2627), en infraestructura · un error real no debe verse como duplicado · asistente propuso, yo aprobé.
- 2026-09-23 · Riesgo aceptado: los logs incluyen el número de documento del paciente · el asistente advirtió que es un dato personal y que los logs circulan más que la base; lo dejo por tiempo y lo mejoraría enmascarándolo · yo.
- 2026-09-23 · **Rechacé permitir contactos de pacientes inactivos, como propuso el asistente; el servicio responde `400`** · si el paciente está inactivo, lo mas logico a mi criterio es no agregarle contactos no tendria sentido, queda a criterio del gestor volver a llamarle para saber qué pasó, sin registrar un contacto · corrección mía; una prueba documenta la regla.
- 2026-09-23 · Contactos con fecha futura se rechazan (`400` antes que `404`); un contacto "No contesta" no cambia `TrackingStatus` porque marcar ilocalizables es el CA-5 y está fuera de alcance · asistente propuso, yo aprobé.
- 2026-09-23 · Listado ordenado del paciente más reciente al más antiguo, con página de 25 (máximo 100) y documento completo · así el paciente recién registrado aparece arriba y el gestor necesita el documento para distinguir pacientes · el asistente propuso orden y 20 por página; yo aprobé el orden y cambié el tamaño a 25.
- 2026-09-23 · **Corregí el orden de `ListAsync`: el asistente contaba primero toda la tabla `Patients` (`COUNT`) y luego traía la página; ahora se trae la página primero y solo se cuenta si es necesario** · si la primera página no se llena, el total es lo que llegó y se ahorra el `COUNT`, que en SQL Server es costoso a gran escala · corrección mía.
- 2026-09-23 · Consulta combinada (Anexo A): `GET /api/patients` pagina `Patients` y, solo para las filas de la página, calcula con dos subconsultas por `PatientId` el total de contactos y el último (`TOP 1` por `ContactDate DESC`), sin contar los anulados (`IsDeleted = 0`) · evita una consulta por paciente (N+1) y evita agrupar toda la tabla `Contacts` para mostrar 25 pacientes · asistente propuso, yo aprobé.
- 2026-09-23 · Índice de esa consulta: `IX_Contacts_PatientId_ContactDate` sobre `(PatientId, ContactDate DESC)` filtrado por `IsDeleted = 0`, para que cada subconsulta entre directo a los contactos del paciente ya ordenados, sin ordenar ni leer anulados. Basta con unos 400 pacientes; con volumen real agregaría `INCLUDE (Channel, ResultCode, GestorUsername)` para no volver a la tabla y un índice `Patients (CreatedAt DESC, Id)` para el orden de la página (migración nueva, no incluida). No medí el plan de ejecución · asistente propuso, yo aprobé.
